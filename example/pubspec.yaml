name: example
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.4.1

# Add regular dependencies here.
dependencies:
  darto: ^0.0.20
  darto_security:
    path: ../

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
